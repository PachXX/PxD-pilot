# CC QA — Claude lane execution pack, run against live 0.2.17

- Date: 2026-08-28
- Executed by: Claude lane
- Source pack: `docs/execution/2026-08-25 - CC Claude lane execution pack.md` (staged `0a88e6dc4d`)
- Live host: `https://mab.pashx.com` · installed app **0.2.17** · host digest
  `sha256:4b249692d377706f56bbf8975a229d9a57deb57063b7eeb738b2dfb64e1c1aca`

## Pack drift (read this first)

The execution pack was written 2026-08-25 and is three days stale in three ways. None of these
are failures; they are context corrections needed to read the results below.

| Pack says | Actual now | Note |
|---|---|---|
| Host `https://34-18-165-1.nip.io` | `https://mab.pashx.com` | nip.io host is dead (TLS fails); domain migrated |
| Installed app `0.2.16` (rollback 0.2.15) | `0.2.17` (rollback 0.2.16) | 0.2.17 published/installed 2026-08-26 |
| Thread 1 redeploy outstanding | **already satisfied** | see Thread 1 |

Evidence doc named for the version actually tested (`0-2-17`) rather than the pack's
`CC-QA-0-2-16.md`, because asserting 0.2.16 results would be false.

## Thread 1 — host redeploy / supplier-RFQ endpoint: **PASS (already satisfied)**

The pack expected this to still return the REST-core `"Query path invalid"` and require a
redeploy. It does not: the host redeploys performed 2026-08-26/27 for the company-identity work
already carried `pashx-supplier-rfq.controller.ts`.

| # | Probe | Expected | Observed | Verdict |
|---|---|---|---|---|
| 1 | `POST …/procurement-cases/:id/supplier-rfqs` with `{}` | typed envelope, `PASHX_INVALID_INPUT`, 5 fieldPaths | `{"ok":false,"code":"PASHX_INVALID_INPUT","fieldPaths":["contractVersion","procurementCaseRecordId","idempotencyKey","expectedVersion","payload"]}` | **PASS** |
| 2 | regression `POST …/transitions` | same typed envelope | identical envelope, same 5 fieldPaths | **PASS** |
| 3 | `/healthz` | 200 | 200 | **PASS** |

No separate redeploy was needed or performed for this thread.

## Thread 2 — §10 Command Centre / operator QA

| # | Check | Pack expectation | Observed | Verdict |
|---|---|---|---|---|
| 1 | Four-signal band | Compliance 0 · Approvals 0 · Blocked **3** · Actions 0 | Compliance 0 · Approvals 0 · Blocked **5** · Actions 0 | **MISMATCH — data drift, explained below** |
| 2 | MAB-PO rail | `vendor-order : current` | `delivery : current` | **MISMATCH — data drift, explained below** |
| 2b | SEN-EPO rail | `invoicing : current` | `invoicing : current` | **PASS** |
| 2c | ASHM rail | `invoicing : current`, DRAFT CPO → readiness missing | `invoicing : current`; readiness gates all "Missing" | **PASS** |
| 3 | Price comparison | single real quote SAR 127,544.20 DRAFT on MAB-PO | `DBMS-QUOTE-STRUCTURAL-MATERIALS · 127,544.20 SAR · Draft`, deterministic-ranking note present | **PASS (exact)** |
| 5 | Vendors | exactly **7** role suppliers | **10** role suppliers | **MISMATCH — see finding 3** |
| 6 | Insights / expenses | honest empty states | "No evidence insights yet…" honest empty state rendered | **PASS** |
| 8 | Bilingual / RTL / a11y | see §Bilingual | all sub-checks pass except target size | **PARTIAL — see finding 4** |

### Finding 1 — Blocked 5 vs 3: two leftover QA fixtures, not a product regression

All five live procurement cases and why each blocks (via `GET /rest/procurementCases`):

| Case | stage | customer | owner |
|---|---|---|---|
| MAB-META-MAB-PO-2026-4141 | DELIVERY | NONE | NONE |
| MAB-META-SEN-EPO-2026-1102 | INVOICING | set | NONE |
| MAB-META-ASHM-004151-1 | INVOICING | set | NONE |
| `vpo-qa-20260826-mta9m5kk Case` | *null* | NONE | NONE |
| `vpo-qa-20260826-mta7ubbz Case` | *null* | NONE | NONE |

The pack's 3 are the three real `MAB-META-*` cases. The extra 2 are **leftover disposable QA
fixtures from another lane's VPO runs that were never cleaned up** — they carry a null stage and
no customer/owner, so they legitimately count as blocked. The Command Centre is reporting stored
state correctly; the signal is inflated by fixture pollution, not by a defect.

**Recommended:** the VPO lane owner should delete `vpo-qa-20260826-mta9m5kk` and
`vpo-qa-20260826-mta7ubbz` (case + supplier each). Not deleted here — they are not this lane's
fixtures and no authority was given for them.

### Finding 2 — MAB-PO rail advanced one stage

Stored `stage` for MAB-META-MAB-PO-2026-4141 is now `DELIVERY`, where the pack froze it at
vendor-order. The UI renders the stored stage faithfully (rail shows intake→vendor-order all
`Complete`, `Delivery` as `Current`, `aria-current="step"` on it). This is a **data change
between 2026-08-25 and now, not a rendering regression.** Per the pack's rule the number was not
adjusted — it is reported as observed.

### Finding 3 — Vendor directory is 10 suppliers, not 7 (**needs a decision**)

Filtered query (`filter=mabBusinessRoles[containsAny]:[SUPPLIER]`) returns 10:

```
Smart Decision Trading Company            V4639129
PowerTech International Trading Co.       V5830512
Sadara Chemical Company                   V7875011   <- not in the original 7
Steel and Metal Solution Trading Company  V2676663   <- not in the original 7
DBMS Steel and Metal Solution Trading Co. V1476195
Attieh Trading Company Ltd                V7290562
L&T C-TEA MYSURU AND MADH                 V1555594   <- not in the original 7
Asia Oruba Trading Co.                    V7695696
Excellence & Success Business Establishment V7846276
Sana for Electrical & Telephone Co.       V8024159
```

`Sadara Chemical Company` and `L&T C-TEA MYSURU AND MADH` are companies **auto-created by the
IMAP mailbox sync** (they carry mail domains `sadara.com` / `larsentoubro.com` and were not part
of the curated supplier set), which then acquired a SUPPLIER role and were consequently issued
vendor IDs by the auto-ID service. This is the vendor-directory-facing consequence of the
contact auto-creation issue already raised separately with Shahil (auto-creation of People and
Companies from every synced mail participant). **Open decision for Shahil**: whether these two
belong in the vendor directory, and whether contact auto-creation should stay on.

Methodology note: an unfiltered `limit=200` read returns a supplier count of 7 and looks like a
pass — but the workspace holds 238 companies, so that read is truncated and the "7" is an
artifact. The filtered count (10) is the correct one.

### Finding 4 — a11y: touch targets below 44px inside the case-workflow content

Scoped to `<main>` (excluding the Twenty shell, which is out of PashX scope): 9 of 14 visible
interactive elements are 21–24px tall — the "Open record" / document-reference links in the
documents ledger and price-comparison tables.

```
فتح سجل الحالة        101 x 24
MAB-PO-2026-4141-RFQ  166 x 21
فتح السجل              62 x 21   (x3, one per ledger row)
DBMS-QUOTE-…          266 x 21
```

Pack criterion is "44px targets". **This row does not pass.** Source-side fix, so it belongs to
the Codex lane. Everything else in check 8 passes (below).

### Bilingual / RTL / accessibility detail (check 8)

| Sub-check | Observed | Verdict |
|---|---|---|
| Arabic toggle | present and functional on both Command Centre and Case workflow | PASS |
| `lang` / `dir` | `<div lang="ar" dir="rtl">`, `getComputedStyle().direction === "rtl"` | PASS |
| Shell stays English/LTR | root `<html lang="en">`, sidebar English; RTL scoped to the `pxd-command` content div | PASS |
| Meaning-matched Arabic | `مركز القيادة`, `مسار المراحل`, `مقارنة الأسعار`, `جاهزية الفوترة` — translated, not transliterated | PASS |
| Canonical values isolated | `SAR`, `MAB-PO-2026-4141`, `DBMS-QUOTE-…` untranslated; numerals localise (`١٢٧٬٥٤٤٫٢٠`) | PASS |
| One H1 | `document.querySelectorAll('h1').length === 1` | PASS |
| Native tables | 2 real `<table>` elements (not div grids) | PASS |
| `aria-current` | `step` on the current rail stage; `page` on the nav item | PASS |
| 200%-equivalent reflow | at 720px: `body.scrollWidth 720 === innerWidth 720`, no horizontal overflow | PASS |
| 44px targets | 9 of 14 in-content targets are 21–24px | **FAIL — finding 4** |

### Refresh / honest-state behaviour (pack check via §10 / WF5 row 10)

Refresh advances the observed timestamp (`Observed 28 Aug 2026, 00:10` → `00:17`) and re-renders
from live data. The transient disabled state was **not observed** at 150ms sampling across 8
samples — the round-trip completes faster than the sampling floor, so "disables then re-enables"
is **unverified, neither pass nor fail**. Empty/loading/partial/error states were not exercised
in this pass.

## Not executed in this pass

- Check 4 (profitability figures) and check 7 (cash band) — not re-derived here.
- Console/runtime health — the browser console tool returned byte-identical output across a
  forced reload, including entries from earlier expired-token navigations, so its output is
  **stale/cached and not trustworthy as evidence**. Recorded as unverified rather than passed.
- Physical-Mac VoiceOver / native Tab order — manual residual, per the pack's own precedent.

## Summary

Thread 1 passes outright. Within Thread 2, every check that tests **rendering fidelity against
stored evidence** passes exactly, including the price comparison to the cent. All three
mismatches are either data drift since the pack was frozen (findings 1–2) or a genuine open
question about auto-created companies entering the vendor directory (finding 3). One real
source-side defect was found: sub-44px touch targets in the case-workflow content (finding 4).
